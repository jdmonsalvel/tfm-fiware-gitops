# Valores de External Secrets Operator
# IRSA_ROLE_ARN se actualiza por el pipeline post-apply (scripts/post-apply.sh)
serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: "arn:aws:iam::575124957370:role/tfm-dev-fiware-gitops-irsa-external-secrets"
installCRDs: true
