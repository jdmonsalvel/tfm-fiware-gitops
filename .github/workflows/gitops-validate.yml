name: GitOps Validate

on:
  pull_request:
    paths:
      - 'gitops/**'
  push:
    branches:
      - main
    paths:
      - 'gitops/**'

permissions:
  contents: read

jobs:
  helm-lint:
    name: Helm Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Helm
        uses: azure/setup-helm@v4
        with:
          version: 'v3.14.4'

      - name: Add Helm Repositories
        run: |
          helm repo add bitnami https://charts.bitnami.com/bitnami
          helm repo update

      - name: Resolve chart dependencies
        run: |
          helm dependency update gitops/charts/orion-ld
          helm dependency update gitops/charts/keyrock
          helm dependency update gitops/charts/wilma

      - name: Lint Orion-LD Chart
        run: helm lint gitops/charts/orion-ld

      - name: Lint Keyrock Chart
        run: helm lint gitops/charts/keyrock

      - name: Lint Wilma Chart
        run: helm lint gitops/charts/wilma

  manifest-validate:
    name: Kubernetes Manifest Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install kubeconform
        run: |
          curl -sSLo kubeconform.tar.gz \
            https://github.com/yannh/kubeconform/releases/latest/download/kubeconform-linux-amd64.tar.gz
          tar xf kubeconform.tar.gz
          sudo mv kubeconform /usr/local/bin/

      - name: Validate ArgoCD Application manifests
        run: |
          kubeconform \
            -schema-location default \
            -schema-location 'https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/{{.Group}}/{{.ResourceKind}}_{{.ResourceAPIVersion}}.json' \
            -summary \
            -verbose \
            gitops/apps/fiware/*.yaml gitops/apps/cluster-config/*.yaml
